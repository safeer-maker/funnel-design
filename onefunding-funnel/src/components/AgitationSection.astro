---
// AgitationSection.astro - Open-Air Staircase Layout (No Cards, No Lines)
---

<section class="section section-subtle agitation-section" id="agitation">
  <div class="container">

    <div class="section-header-clean">
      <span class="section-kicker">THE COST OF INACTION</span>
      <h2>
        Every Day You Wait, This Is What It's Costing You
      </h2>
      <p>
        The aggregator model was built to protect their risk, not your margins. These aren't estimates — they're the mathematical reality of flat-rate pricing at scale.
      </p>
    </div>

    <!-- ===== OPEN-AIR STAIRCASE LAYOUT ===== -->
    <div class="staircase-flow">

      <!-- Step 1: Immediate Loss -->
      <div class="staircase-step step-tier-1" data-metric>
        <div class="step-meta">
          <span class="step-num">01</span>
          <span class="step-phase">IMMEDIATE LOSS</span>
        </div>

        <div class="step-metric-block">
          <div class="agit-number-wrap">
            <div class="agit-big-num text-rose">
              $<span class="count-up" data-target="22000" data-format="currency">0</span>
            </div>
            <div class="agit-tag tag-rose">ANNUAL FEE OVERPAYMENT</div>
          </div>

          <!-- Bar visual -->
          <div class="agit-visual">
            <div class="metric-bar-bg">
              <div class="metric-bar-fill bar-rose" style="width: 0%" data-target-width="78%"></div>
            </div>
            <div class="metric-bar-labels">
              <span>You pay: 3.5%</span>
              <span>True cost: ~1.4%</span>
            </div>
          </div>
        </div>

        <div class="step-content-block">
          <h3 class="agit-heading">Overpaid in Flat-Rate Markups</h3>
          <p class="agit-copy">
            Aggregators pocket the spread between ~1.4% wholesale interchange and the 3.5% they charge you — silently, on every single transaction. The more you grow, the more you lose.
          </p>
        </div>
      </div>

      <!-- Step 2: Systemic Risk -->
      <div class="staircase-step step-tier-2" data-metric>
        <div class="step-meta">
          <span class="step-num">02</span>
          <span class="step-phase">SYSTEMIC RISK</span>
        </div>

        <div class="step-metric-block">
          <div class="agit-number-wrap">
            <div class="agit-big-num text-rose">
              <span class="count-up" data-target="25" data-format="percent">0</span>
            </div>
            <div class="agit-tag tag-rose">ACCOUNT FREEZE RISK</div>
          </div>

          <!-- Frozen dot grid -->
          <div class="agit-visual">
            <div class="frozen-grid">
              <span class="frozen-dot dot-red" title="Frozen account"></span>
              <span class="frozen-dot dot-gray"></span>
              <span class="frozen-dot dot-gray"></span>
              <span class="frozen-dot dot-gray"></span>
            </div>
            <span class="frozen-caption">1 in every 4 scaling merchants</span>
          </div>
        </div>

        <div class="step-content-block">
          <h3 class="agit-heading">Scaling Accounts Capped or Frozen</h3>
          <p class="agit-copy">
            Automated AI risk bots — not humans — decide your fate. A sudden sales spike or one chargeback cluster triggers instant volume caps with zero consultation, zero warning, zero recourse.
          </p>
        </div>
      </div>

      <!-- Step 3: Growth Bottleneck -->
      <div class="staircase-step step-tier-3" data-metric>
        <div class="step-meta">
          <span class="step-num">03</span>
          <span class="step-phase">GROWTH BOTTLENECK</span>
        </div>

        <div class="step-metric-block">
          <div class="agit-number-wrap">
            <div class="agit-big-num text-rose">
              <span class="count-up" data-target="14" data-format="days">0</span>
            </div>
            <div class="agit-tag tag-rose">CASH FLOW DRAG</div>
          </div>

          <!-- Days track -->
          <div class="agit-visual">
            <div class="days-track">
              <div class="day-block day-locked"></div>
              <div class="day-block day-locked"></div>
              <div class="day-block day-locked"></div>
              <div class="day-block day-locked"></div>
              <div class="day-block day-locked"></div>
              <div class="day-block day-locked"></div>
              <div class="day-block day-locked"></div>
              <div class="day-block day-locked"></div>
              <div class="day-block day-locked"></div>
              <div class="day-block day-locked"></div>
              <div class="day-block day-locked"></div>
              <div class="day-block day-locked"></div>
              <div class="day-block day-locked"></div>
              <div class="day-block day-locked"></div>
              <div class="day-block day-free"></div>
            </div>
            <div class="days-legend">
              <span><span class="legend-dot dot-red-sm"></span> Locked 14 days by aggregator</span>
              <span><span class="legend-dot dot-green-sm"></span> OneFunding: next business day</span>
            </div>
          </div>
        </div>

        <div class="step-content-block">
          <h3 class="agit-heading">Delayed Access to Your Working Capital</h3>
          <p class="agit-copy">
            Multi-day settlement lags lock your earned revenue away — preventing you from reinvesting into ads, inventory, and growth opportunities your competitors are capturing right now instead of you.
          </p>
        </div>
      </div>

    </div>

    <!-- CTA Row -->
    <div class="agitation-cta-row">
      <p class="agitation-cta-text">Every day on flat-rate pricing is cash flowing out of your business into aggregator profits.</p>
      <a href="#auditForm" class="btn btn-primary btn-lg">
        <span>Stop the Bleeding — Get My Free Rate Report →</span>
      </a>
    </div>

  </div>
</section>

<style>
  .agitation-section {
    position: relative;
  }

  .section-kicker {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 800;
    letter-spacing: 0.1em;
    color: var(--rose-primary);
    text-transform: uppercase;
    margin-bottom: 0.75rem;
  }

  /* ===== OPEN-AIR STAIRCASE CONTAINER ===== */
  .staircase-flow {
    max-width: 980px;
    margin: 0 auto 4rem auto;
    display: flex;
    flex-direction: column;
    gap: 3.5rem;
  }

  /* ===== OPEN-AIR STEP (NO CARDS, NO BORDERS, NO BOX-SHADOWS) ===== */
  .staircase-step {
    display: grid;
    grid-template-columns: 110px 240px 1fr;
    gap: 2.5rem;
    align-items: flex-start;
    padding: 1rem 0;
    opacity: 0;
    transform: translateY(20px);
    transition: opacity 0.55s ease, transform 0.55s ease;
  }

  .staircase-step.is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* Cascading Staircase Indentations (Desktop) */
  @media (min-width: 960px) {
    .step-tier-1 {
      margin-left: 0;
      max-width: 820px;
    }

    .step-tier-2 {
      margin-left: 5rem;
      max-width: 820px;
    }

    .step-tier-3 {
      margin-left: 10rem;
      max-width: 820px;
    }
  }

  @media (min-width: 768px) and (max-width: 959px) {
    .step-tier-1 {
      margin-left: 0;
    }

    .step-tier-2 {
      margin-left: 2.5rem;
    }

    .step-tier-3 {
      margin-left: 5rem;
    }
  }

  /* Step Meta / Phase */
  .step-meta {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
    padding-top: 0.25rem;
  }

  .step-num {
    font-family: var(--font-display);
    font-size: 1.6rem;
    font-weight: 800;
    color: var(--text-light);
    line-height: 1;
    letter-spacing: -0.02em;
  }

  .step-phase {
    font-size: 0.65rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    color: var(--rose-primary);
    line-height: 1.3;
  }

  /* Metric Column */
  .step-metric-block {
    display: flex;
    flex-direction: column;
    gap: 0.85rem;
  }

  .agit-number-wrap {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
  }

  .agit-big-num {
    font-family: var(--font-display);
    font-size: 2.85rem;
    font-weight: 900;
    line-height: 1;
    letter-spacing: -0.04em;
  }

  .text-rose {
    color: var(--rose-primary);
  }

  .agit-tag {
    font-size: 0.65rem;
    font-weight: 800;
    letter-spacing: 0.07em;
    padding: 0.2rem 0.55rem;
    border-radius: var(--radius-sm);
    width: fit-content;
  }

  .tag-rose {
    background: var(--rose-bg);
    color: var(--rose-primary);
  }

  /* Content Column */
  .step-content-block {
    display: flex;
    flex-direction: column;
    padding-top: 0.2rem;
  }

  .agit-heading {
    font-size: 1.25rem;
    color: var(--text-heading);
    font-weight: 700;
    margin-bottom: 0.55rem;
    line-height: 1.3;
  }

  .agit-copy {
    font-size: 0.95rem;
    color: var(--text-body);
    line-height: 1.68;
  }

  /* ===== MINI VISUALIZATIONS ===== */
  .agit-visual {
    width: 100%;
  }

  /* Bar */
  .metric-bar-bg {
    height: 7px;
    background: var(--rose-bg);
    border-radius: 4px;
    overflow: hidden;
    margin-bottom: 0.35rem;
  }

  .metric-bar-fill {
    height: 100%;
    border-radius: 4px;
    transition: width 1.4s cubic-bezier(0.22, 1, 0.36, 1);
  }

  .bar-rose { background: var(--rose-primary); }

  .metric-bar-labels {
    display: flex;
    justify-content: space-between;
    font-size: 0.7rem;
    font-weight: 600;
    color: var(--text-muted);
  }

  /* Frozen dots */
  .frozen-grid {
    display: flex;
    gap: 0.45rem;
    margin-bottom: 0.35rem;
  }

  .frozen-dot {
    width: 24px;
    height: 24px;
    border-radius: 6px;
    flex-shrink: 0;
  }

  .dot-red {
    background: #fecdd3;
    position: relative;
  }

  .dot-red::after {
    content: '✕';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-size: 0.65rem;
    font-weight: 900;
    color: var(--rose-primary);
  }

  .dot-gray {
    background: var(--bg-muted);
  }

  .frozen-caption {
    font-size: 0.7rem;
    font-weight: 600;
    color: var(--text-muted);
    display: block;
  }

  /* Days track */
  .days-track {
    display: flex;
    gap: 2px;
    margin-bottom: 0.45rem;
  }

  .day-block {
    flex: 1;
    height: 10px;
    border-radius: 2px;
  }

  .day-locked { background: #fecdd3; }
  .day-free   { background: var(--emerald-primary); }

  .days-legend {
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
  }

  .days-legend span {
    display: flex;
    align-items: center;
    gap: 0.3rem;
    font-size: 0.69rem;
    font-weight: 600;
    color: var(--text-muted);
  }

  .legend-dot {
    width: 9px;
    height: 9px;
    border-radius: 2px;
    flex-shrink: 0;
  }

  .dot-red-sm   { background: #fecdd3; }
  .dot-green-sm { background: var(--emerald-primary); }

  /* ===== BOTTOM CTA ===== */
  .agitation-cta-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
    max-width: 980px;
    margin: 0 auto;
    padding: 2rem 2.5rem;
    background: var(--bg-dark);
    border-radius: var(--radius-xl);
    flex-wrap: wrap;
  }

  .agitation-cta-text {
    font-size: 1rem;
    font-weight: 600;
    color: #cbd5e1;
    max-width: 500px;
    line-height: 1.55;
  }

  /* ===== RESPONSIVE ===== */
  @media (max-width: 768px) {
    .staircase-flow {
      gap: 2.75rem;
    }

    .staircase-step {
      grid-template-columns: 1fr;
      gap: 1.25rem;
      padding: 0;
      margin-left: 0 !important;
      max-width: 100% !important;
    }

    .step-meta {
      flex-direction: row;
      align-items: baseline;
      gap: 0.65rem;
    }

    .step-num {
      font-size: 1.3rem;
    }

    .step-metric-block {
      width: 100%;
    }

    .agit-big-num {
      font-size: 2.35rem;
    }

    .agitation-cta-row {
      flex-direction: column;
      text-align: center;
      padding: 1.75rem;
    }

    .agitation-cta-row .btn {
      width: 100%;
    }
  }
</style>

<script>
  function initAgitation() {
    const steps = document.querySelectorAll('[data-metric]');

    function easeOutQuart(t: number): number {
      return 1 - Math.pow(1 - t, 4);
    }

    function animateCountUp(el: Element) {
      const spanEl = el.querySelector('.count-up') as HTMLElement;
      if (!spanEl) return;

      const target = parseFloat(spanEl.dataset.target || '0');
      const format = spanEl.dataset.format || 'number';
      const duration = 1400;
      const startTime = performance.now();

      function formatValue(val: number): string {
        if (format === 'currency') return Math.round(val).toLocaleString('en-US');
        if (format === 'percent') return Math.round(val) + '%';
        if (format === 'days')    return Math.round(val) + ' Days';
        return Math.round(val).toLocaleString('en-US');
      }

      function tick(now: number) {
        const elapsed = now - startTime;
        const progress = Math.min(elapsed / duration, 1);
        spanEl.textContent = formatValue(easeOutQuart(progress) * target);
        if (progress < 1) requestAnimationFrame(tick);
        else spanEl.textContent = formatValue(target);
      }

      requestAnimationFrame(tick);

      // Animate bar if present
      const bar = el.querySelector('.metric-bar-fill') as HTMLElement;
      if (bar) {
        setTimeout(() => { bar.style.width = bar.dataset.targetWidth || '0%'; }, 150);
      }
    }

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            const index = Array.from(steps).indexOf(entry.target);
            setTimeout(() => {
              entry.target.classList.add('is-visible');
              animateCountUp(entry.target);
            }, index * 160);
            observer.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.2 }
    );

    steps.forEach((s) => observer.observe(s));
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initAgitation);
  } else {
    initAgitation();
  }
</script>
